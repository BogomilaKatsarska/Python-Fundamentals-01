temps = [21, 24, 25, 18, 12]
# Indexes - how we can access elements
# The length is the number of elements
student = {"name": "Ines", "age": 26}
# Strings are immutable data types - cannot be changed

# first_name = "Ines"
# first_name = first_name.replace("n", "m")
# print(first_name)

# 'in' - item in sequence
# if 'n' in 'banana':
#     print("N is in banana")

# chr(91) --> Converts ascii values to numbers/letters (ord=ordinal)
# ord("A") --> Converts numbers/letters to ascii values

# .lower() and .upper() methods of strings

# A list contains items separated by commas and enclosed within square brackets
# A tuple is a collection which is ordered and unchangeable(immutable).
# A set is a collection which is unordered and unindexed. Set has only unique elements. Sets are written in curly brackets
# A dictionary is a collection which is unordered, changeable and indexed. They have keys and values